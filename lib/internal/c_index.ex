# defmodule C_Index do
#   def calc_validity(datasets) do
#     s_w =
#       datasets.each_amount
#       |> Enum.map(fn(amount) -> (amount*(amount-1)) / 2 end)
#       |> Enum.sum
#     s_min = 
#     s_max = 
#   end
# end

